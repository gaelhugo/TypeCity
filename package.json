{
  "dependencies": {
    "@onemorestudio/playgroundjs": "^1.0.0",
    "babel-plugin-transform-remove-console": "^6.9.4",
    "firebase": "^9.14.0",
    "parcel": "*",
    "parcel-reporter-static-files-copy": "^1.3.0",
    "three": "^0.147.0"
  },
  "name": "typecity",
  "version": "1.0.0",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0"
  },
  "scripts": {
    "start": "rm -rf dist && parcel index.html --port 80 --host 0.0.0.0 --open",
    "start-https": "rm -rf dist && parcel index.html --cert certificate.cert --key certificate.key --port 80 --open",
    "build": "rm -rf dist && parcel build index.html --public-url ./"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
